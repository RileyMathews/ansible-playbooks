services:
  homebridge:
    image: homebridge/homebridge:2025-02-26
    restart: always
    network_mode: host
    volumes:
      - ./data/homebridge:/homebridge
    logging:
      driver: json-file
      options:
        max-size: "10mb"
        max-file: "1"
