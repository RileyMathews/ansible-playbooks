# configure_git/tasks/main.yml
---
- name: Set Git user name
  git_config:
    name: user.name
    value: "{{ git_user_name }}"
    scope: global

- name: Set Git user email
  git_config:
    name: user.email
    value: "{{ git_user_email }}"
    scope: global

